import assert from 'node:assert/strict';function f(x){if(!x)return'?';const s=String(x.status||'').toUpperCase();if(s==='DOWN')return'down';if(s==='CLOSED'||s==='REFURBISHMENT')return'fechado';const w=x.queue?.STANDBY?.waitTime;return Number.isFinite(w)?String(w):'?'}assert.equal(f({status:'OPERATING',queue:{STANDBY:{waitTime:45}}}),'45');assert.equal(f({status:'DOWN'}),'down');assert.equal(f({status:'CLOSED'}),'fechado');assert.equal(f({status:'REFURBISHMENT'}),'fechado');assert.equal(f(null),'?');console.log('OK: 5 testes passaram.');